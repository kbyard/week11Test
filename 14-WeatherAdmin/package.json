{
  "name": "14-weatheradmin",
  "version": "1.0.0",
  "description": "* Implement the logic for the `WeatherAdmin` component. As with the `UserSearch` component, you should start with pseudocode.",
  "main": "UserSearch.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "weather-js": "^2.0.0"
  }
}
